import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: '40978-PWM-Sprint-4-Ionic-Angular',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
